<h1>Create a new review</h1>

<%= simple_form_for([@performance, @review]) do |f| %>
  <%= f.input :description %>
  <%= f.input :rating, collection: (1..5).to_a, label: "Rate your experience" %>
  <%= f.submit "Post review" %>
<% end %>
